#pragma once

const int N = 8; //кол-во процессов
const char* LPSZCLASSNAME = "MyClass";
const char* LPWINDOWNAME = "Global minimum";
const char* F_STRING = "f = sin(5 * pow(x, 3)) + cos(7 * pow(x, 4)) * x";
